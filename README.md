# Flutter VSCode 세팅

## 프로젝트 폴더 생성

## GIT 연결

`git init`
`git commit -m <"커밋내용">`
`git branch -M main`
`git remote add origin <URL>` 명령어를 통해 remote한다.
`git remote -v`
`git push -u origin main`


## downlod SDK

원하는 경로에 SDK를 설치한다.