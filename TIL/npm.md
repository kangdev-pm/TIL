### npm start
package.json 파일을 확인하여
"scripts" 부분의 명령어를 확인한다.
보통 `npm run start`, `npm run serve` 등으로 되어있다.

### npm install
package.json 파일을 확인하여
"dependencies" 부분의 코드를 확인한다.
npm install을 하면 dependencies에 작성된 패키지가 install된다.

